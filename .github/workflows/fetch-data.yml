name: Fetch AI Developments Data

on:
  schedule:
    # Runs daily at 06:00 UTC. Adjust as needed.
    - cron: '0 */2 * * *'
  workflow_dispatch: {}   # allows manual trigger from the Actions tab

permissions:
  contents: write
  models: read

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch RSS sources
        run: python scripts/fetch_rss.py

      - name: Fetch scraped sources (no RSS available)
        run: python scripts/fetch_scrape.py

      - name: Fetch arXiv papers
        run: python scripts/fetch_arxiv.py

      - name: Generate daily summaries
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/summarize.py

      - name: Commit updated data
        run: |
          git config user.name "ai-dashboard-bot"
          git config user.email "actions@github.com"
          git add data/articles/*.json data/summaries/*.json
          git diff --quiet --cached || git commit -m "Scheduled data refresh"
          git push
